import ElementsContainer from "../ElementsContainer/ElemenetsContainer";import {getItems, setItems} from "../../Services/ManageData.service";import {useState} from "react";type ShowElementsType  = {    numberOfElements: number}const ShowElements = ({numberOfElements = 0}: ShowElementsType) => {    const [listOfElements] = useState<number[] | string[]>(() => {        setItems(numberOfElements);        return getItems();    });    return (        <ElementsContainer listOfElements={listOfElements} />    )}export default ShowElements;