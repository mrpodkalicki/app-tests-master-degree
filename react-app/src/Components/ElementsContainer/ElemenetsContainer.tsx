import './ElementsContainer.css'type ElementsContainerType = {    listOfElements: number[] | string[];}const ElementsContainer = ({listOfElements} :ElementsContainerType) => {    const showElements = listOfElements.map((number, index) => <p key={index}>{number}</p>);    return (        <div className='container'>                {showElements}        </div>    )}export default ElementsContainer;