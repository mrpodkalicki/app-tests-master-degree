import './ElementsContainer.css'type ElementsContainerType = {    listOfElements: number[];}const ElementsContainer = ({listOfElements} :ElementsContainerType) => {    const showElements = listOfElements.map((number, index) => <div key={index}>{number}</div>);    return (        <div className='container'>                {showElements}        </div>    )}export default ElementsContainer;