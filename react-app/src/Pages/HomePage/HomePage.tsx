import {useNavigate} from "react-router-dom";import {    CREATE_DELETE_UPDATE_TEST,    FIVE_HUNDRED_THOUSAND,    LAZY_LOADING_TEST,    ONE_HUNDRED_THOUSAND,    TEN_THOUSAND} from "../../utils/uiUrl";import './HomePage.css';const HomePage = () => {    const navigate = useNavigate();    const navigateTo = (url: string) => navigate(`${url}`);    return (        <div>            <p>Initial tests</p>            <div className="btn-wrapper">                <button onClick={() => navigateTo(`/${TEN_THOUSAND}`)}>Go to 10 000</button>                <button onClick={() => navigateTo(`/${ONE_HUNDRED_THOUSAND}`)}>Go to 100 000</button>                <button onClick={() => navigateTo(`/${FIVE_HUNDRED_THOUSAND}`)}>Go to 500 000</button>            </div>            <p>lazy-loading</p>            <div className="btn-wrapper">                <button onClick={() => navigateTo(`/${LAZY_LOADING_TEST}/${TEN_THOUSAND}`)}>Go to 10 000</button>                <button onClick={() => navigateTo(`/${LAZY_LOADING_TEST}/${ONE_HUNDRED_THOUSAND}`)}>Go to 100 000</button>                <button onClick={() => navigateTo(`/${LAZY_LOADING_TEST}/${FIVE_HUNDRED_THOUSAND}`)}>Go to 500 000</button>            </div>            <p>Create / update</p>            <div className="btn-wrapper">                <button onClick={() => navigateTo(CREATE_DELETE_UPDATE_TEST)}>Go to Create/ updates tests</button>            </div>        </div>    )};export default HomePage;